services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inworld-tts-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./output:/app/output
    environment:
      - TZ=America/Sao_Paulo
